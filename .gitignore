node_modules/
dist/
*.local
.DS_Store
.vite/

# Generated by `node scripts/make-samples.mjs` — uncompressed BMPs, not worth
# committing when a script reproduces them exactly.
samples/test-*.bmp

# A stock off-image copied off a calculator. SwissMicros' artwork, so it stays
# out of the repo; samples/reference-header.bin carries the format facts the
# tests actually need. See test/referenceOffimg.test.ts.
samples/reference-offimg.bmp
